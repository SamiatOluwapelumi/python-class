#select distinct is used to return only distinct(different) values.
#example:  select distinct column from table_name;
#to list the number of different(distinct) customer country:  SELECT COUNT(DISTINCT country) FROM customers; 
   # SQL WHERE;
# to filter records. to extract only those records that fufil a specified condition.
#syntax; SELECT column FROM table_name WHERE condition;
# EX: SELECT * FROM customers WHERE country='mexico';